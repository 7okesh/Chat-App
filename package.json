{
  "name": "chitchat-app",
  "version": "2.0.0",
  "description": "Real-time chat application powered by Node.js, Express, and Socket.IO",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node -e \"const http=require('http'); require('./server.js'); setTimeout(()=>{ http.get('http://localhost:8000/health', (r)=>{ if(r.statusCode===200){ console.log('Server test passed!'); process.exit(0); } else { process.exit(1); } }); }, 1500);\""
  },
  "keywords": [
    "chat-app",
    "socket.io",
    "nodejs",
    "realtime",
    "express"
  ],
  "author": "7okesh",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
